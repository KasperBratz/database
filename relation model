Customer(!Name,Address);

Orders(!OrderNbr, DeliveryTime, \CustomerName, Delivered);

Pallets(!PalletNbr, TimeMade, \OrderNbr, Blocked, \cookieName, Sent);


Cookies(!Name);

Recipe(!\RawMaterial, !\CookieName, Amount);

RawMaterials(!RawMaterial, Amount, LastDeliveryDate, LastDeliveryAmount);
